# 内容分诊台 —— 使用说明

一个在你自己电脑上跑的小工具：**贴一段内容，它给出参考判断**（属于哪一类、多急、要不要人工介入）。

支持 **中文 / English / Bahasa Melayu** 三种界面，内容也不限语言。

---

## ⚠️ 先看这一条：它不是自动判定系统

这个工具**只提供参考信号，不能用来做自动处置**。这不是谦虚，是实测出来的硬限制：

> 同一段内容，**只把某个分类的描述换个说法**，判定结果就会变。
> 例如把 `程序错误、服务中断或集成问题` 改成 `故障报修，程序报错或服务中断`，
> 同一段退款投诉就从「退款」变成了「故障报修」——而两次给出的"可能性百分比"几乎一样（97.5% vs 97.7%）。

**所以界面上那个百分比不代表准确率。** 唯一还有点参考价值的是 **「两种问法一致 / 不一致」** 那个标记——不一致时尤其不要采信。

实测准确率参考：

| 内容语言 | 准确率 |
|---|---|
| 英文 | 4/4（可用） |
| 马来文 | 3/4 |
| 中文 | 4/9（偏弱） |

**任何对外发出的动作，都必须由人确认后再做。**

---

## 一、安装（2 步，不需要预装任何东西）

### 第 1 步：双击 `install.bat`

**不需要先装 Python。** 有没有都能用：

| 你的电脑 | install.bat 会怎么做 |
|---|---|
| **没有 Python** | 自动下载一个便携版（约 38 MB），放在本文件夹的 `.bootstrap\` 里 |
| 有 Python 3.10+ | 直接用它 |
| 有 Python 但太旧（3.9 及以下） | 当成没有，走上面的便携版 |

便携版 Python **不会装进你的系统、不会改 PATH、不需要管理员权限**，全部在本文件夹内。不喜欢的话删掉整个 `.bootstrap\` 文件夹就干净了。

它会依次做四件事：
1. 准备 Python（见上表）
2. 在本文件夹里建一个独立的虚拟环境 `.venv`
3. 检测你有没有 NVIDIA 显卡，装对应版本的 PyTorch
4. 装好其余依赖

**时间取决于显卡：**
- 有 NVIDIA 显卡：要下载约 **2.5–3 GB**，慢的话十几分钟
- 没有显卡：只下约 **250 MB**

> 如果安装中途失败（多半是网络问题），重新双击 `install.bat` 即可，已下载的部分不会白费。
> 想强制装 CPU 版：命令行运行 `install.bat --cpu`

### 第 2 步：双击 `start.bat`

**浏览器会自动打开**，不需要你做任何事。会看到一个黑色窗口显示：

```
============================================================
  内容分诊台 正在启动，浏览器会自动打开…
  页面地址：http://127.0.0.1:8100/
============================================================
  如果浏览器没有自动弹出，请手动把上面的地址复制到浏览器。
  页面第一次打开会显示「正在加载模型…」，
  首次启动要从网上下载模型（约 1.5 GB），请耐心等几分钟；
  变成「模型就绪」就可以开始用了。

  停止运行：点这个窗口按 Ctrl+C，或直接关掉这个窗口。
```

**这个黑窗口不要关**，关掉服务就停了。用完再关。

> **第一次启动会下载模型权重（约 1.5 GB），要等几分钟。**
> 页面会显示"正在加载模型…"，下载完就变成"模型就绪"。
> 之后每次启动只要几秒（权重已经存在本地了）。

**几个贴心设计，你可能用得上：**

- 浏览器没自动弹出来？把黑窗口里那行 `http://127.0.0.1:8100/` 复制到浏览器就行
- **不小心双击了两次 `start.bat`？** 不会出问题，第二次会直接把你已经开着的页面调出来，然后自己退出
- 关窗口时如果显示"启动失败"，多半是端口被占了，黑窗口里有中文提示告诉你怎么做

---

## 二、怎么用

1. 把内容贴进输入框
2. 点「分析」（或按 `Ctrl + Enter`）
3. 看结果

结果里有四块：

| 区域 | 看什么 |
|---|---|
| **参考判定** | 最可能的分类，以及两种问法是否一致 |
| **分类可能性分布** | 各个分类的相对可能性 |
| **参考信号** | 紧急程度、负面情绪、是否需人工介入、是否有害 |
| **技术细节**（可展开） | 每个分类原始的 P(是)，以及用了哪条语言通道 |

右上角可以切换**界面语言**（中文 / English / Bahasa Melayu）。
切换后**所有输出都跟着变**，跟你输入的内容是什么语言无关。

输入框下面有三个示例按钮，可以点一下看看效果。

---

## 三、改分类和问题

打开页面，上方有两个标签：

| 标签 | 干什么 |
|---|---|
| **分析** | 贴内容、看结果 |
| **问题与选项** | **全部设置都在这里**：Q1 是分类问题（它的「选项」就是分类本身），Q2 起是附加问题（模型实际被问什么），可增删改、可换类型 |

> 「分类管理」已经并入「问题与选项」——因为分类就是 Q1 这个多选一问题的选项，
> 分成两个页面反而不好理解。

改完点「**保存**」**立即生效，不用重启**。改完建议点「**提问预览**」看一眼模型实际收到的提问。

详细操作见同目录下的 **`app\分类维护说明.md`**。

两条必须知道的规则：
1. **`other` 那一项是锁定的**（显示 🔒 固定不可改）：没有删除按钮、启用勾选框是灰的、标识是只读的，
   名称和描述还能改。它承接"不属于任何分类"的内容，所以系统不让你动它
2. **改「描述」或「提问」的文字会改变判定结果**，改完必须用真实内容重新测

---

## 四、常见问题

### 没有 Python 也能装吗

**能。** 直接双击 `install.bat` 就行，它会自动下载一个便携版 Python 放在 `.bootstrap\` 里，
不装进系统、不改 PATH、不需要管理员权限。

### 安装时报「引导程序校验失败（SHA256 不匹配）」

说明 uv 那个文件没下载完整（网络中断）。删掉 `.bootstrap` 文件夹重新双击 `install.bat` 即可。

### 提示「取到的 Python 不在本包的 .bootstrap 目录内，已中止」

这是保护机制：说明系统把别的 Python 认成了我们的便携版。
删掉 `.bootstrap` 文件夹后重新双击 `install.bat`；若反复出现，把这段报错发给我。

### 安装时报网络错误 / 卡住不动

多试几次。PyTorch 有 2.5–3 GB，网络不稳会断。
实在装不上就先装 CPU 版：命令行运行 `install.bat --cpu`（只有 250 MB）。

### 启动后浏览器打不开 127.0.0.1:8100

1. 确认 `start.bat` 那个黑窗口还开着（关掉窗口服务就停了）
2. 黑窗口里那行 `页面地址：http://...` 直接复制到浏览器地址栏
3. 如果黑窗口提示**「启动失败。最常见的原因是端口 8100 已经被别的程序占用」**，
   按它给的两条建议做即可（关掉之前的窗口，或者换个端口）

### 双击 start.bat 提示「启动失败」/ 端口被占用

最常见的原因：**你之前开的那个窗口还在（可能是最小化的）**。
它可能藏在任务栏里。找到它，关掉，再重新双击 `start.bat`。

如果你确实想同时开两个（比如一个自己用一个给同事），换个端口：
```
set APP_PORT=8200
.venv\Scripts\python.exe app\server.py
```
然后浏览器打开 http://127.0.0.1:8200

### 不小心双击了两次 start.bat，会有问题吗

没问题。第二次会检测到服务已经在跑，直接把你已经开着的页面调出来，
然后自己退出，不会启动第二个服务、也不会报错。

### 一直显示「正在加载模型…」

第一次启动要下载 1.5 GB 权重，慢是正常的。看黑窗口里有没有下载进度条。
如果超过半小时还没好，大概率是网络连不上 HuggingFace，重新启动一次试试。

### 提示「模型加载失败」

把黑窗口里的完整报错发出来。
最常见的原因是显存不够——4GB 以下的显卡比较紧张，可以关掉浏览器、游戏、壁纸软件再试。

### 分析速度很慢（一条要好几秒）

说明在用 CPU 跑，可能是没装 GPU 版 PyTorch，或者显存不够自动退回了 CPU。
确认一下：命令行运行
```
.venv\Scripts\python.exe -c "import torch; print(torch.cuda.is_available())"
```
输出 `True` 才是用显卡。是 `False` 就重装：`install.bat --cuda cu126`

### 页面上出现红色的「分类配置有错误」

你在分类管理里改的东西没通过校验。页面会逐条写清楚哪个分类、哪个字段有问题。
**此时系统会自动沿用上一份保存成功的配置**，不会崩，慢慢改就好。

### 想让同事也能访问（局域网）

默认只绑本机（`127.0.0.1`），别人访问不到——这是安全的默认值。

要开放给局域网，启动前设置：
```
set APP_HOST=0.0.0.0
set LAYA_API_KEY=自己定一个密码
.venv\Scripts\python.exe app\server.py
```
**务必同时设置 `LAYA_API_KEY`**，否则同一网络下任何人都能免密码使用你的电脑。

### 怎么完全卸载

直接删掉整个文件夹就行。不会在系统里留任何东西。

安装过程中产生的东西全在这个文件夹内，跟着一起删掉了：
`.venv\`（依赖）、`.bootstrap\`（便携版 Python 和 uv）。

（模型权重缓存在 `C:\Users\你的用户名\.cache\huggingface`，要一并删掉的话手动删除那个目录里的 `models--convaiinnovations--laya` 文件夹。）

---

## 五、文件说明

| 文件 | 说明 |
|---|---|
| `install.bat` / `install.py` | 安装程序 |
| `bootstrap.ps1` | 没装 Python 时自动下载便携版 Python 用的（一般不用管） |
| `start.bat` / `start.sh` | 启动程序（Windows / Linux+macOS） |
| `requirements.txt` | 依赖清单（一般不用管） |
| `app\server.py` | 程序主体 |
| `app\categories.json` | 分类配置 |
| `app\static\` | 网页界面文件 |
| `app\分类维护说明.md` | **怎么改分类（发给用这个工具的同事看）** |
| `app\示例-*.csv` | 整套分类模板，可在分类管理页导入 |

---

## 六、技术背景（想了解的话）

- 引擎是 [Laya](https://huggingface.co/convaiinnovations/laya)（`convaiinnovations/laya`），
  一个 421M 参数的非自回归决策模型，Apache 2.0 许可，可商用
- 用的是它的 multilingual 检查点（322M，支持 100+ 语言）
- 判定方式：**同一次计算里问两组问题**（一次多选 + 一组是非题），
  两组结果一致才标"一致"
- 全部在本机运行，**内容不会上传到任何地方**
- 模型需要联网下载一次，之后完全离线可用
